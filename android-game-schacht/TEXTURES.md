# Boden-Texturen

Die Kacheln in `app/src/main/res/drawable-nodpi/tile_*.png` (Wasser + Gras in
mehreren Reichtums-Stufen) wurden mit `tools/gen_tiles.py` prozedural erzeugt
und sind Eigenerstellung — freigegeben als **CC0 / Public Domain**. Keine
externen/urheberrechtlich geschuetzten Bilder.

Neu erzeugen: `python3 tools/gen_tiles.py` (benoetigt Pillow).
